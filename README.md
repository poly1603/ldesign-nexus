# LDesign Nexus

LDesign 桌面管理应用 - 基于 Flutter 开发的跨平台桌面应用

## 前置要求

- Flutter SDK >= 3.0.0
- Dart SDK >= 3.0.0
- Windows/macOS/Linux 操作系统

## 快速开始

### 1. 安装依赖

```bash
cd tools/nexus
flutter pub get
```

### 2. 启动应用

**生产模式（默认）：**
```bash
flutter run -d windows
```

**Watch 模式（热重载，推荐开发时使用）：**
```bash
# Windows PowerShell
.\scripts\watch.ps1

# Linux/macOS
chmod +x scripts/watch.sh
./scripts/watch.sh

# 或直接使用 Flutter 命令
flutter run -d windows --hot
```

**Watch 模式特性：**
- ✅ 代码修改后自动热重载（无需手动重启）
- ✅ 支持按 `r` 键手动刷新
- ✅ 支持按 `R` 键完全重启应用
- ✅ 按 `q` 键退出

### 3. 后端服务器

应用会自动尝试启动后端服务器（如果 `tools/server` 存在）。如果自动启动失败，你可以手动启动：

```bash
# 在项目根目录
cd tools/server
pnpm start:dev
```

或者使用 CLI：
```bash
pnpm --filter @ldesign/cli ui --server-only --no-open
```

## 开发说明

- 应用默认窗口大小：1200x800
- 最小窗口大小：800x600
- 后端服务器端口：3000
- 健康检查端点：http://localhost:3000/api/health

## 功能特性

- 🏠 首页仪表板
- 📦 项目管理
- 🌐 Node.js 版本管理
- 🔧 系统信息
- 📝 日志查看
- ⚙️ 设置配置