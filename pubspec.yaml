name: ldesign_nexus
description: LDesign Desktop Application - 桌面管理应用
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # UI
  cupertino_icons: ^1.0.6
  flutter_riverpod: ^2.4.0
  go_router: ^13.0.0
  
  # HTTP & WebSocket
  dio: ^5.4.0
  socket_io_client: ^2.0.3+1
  
  # System
  path: ^1.9.0
  path_provider: ^2.1.0
  process_run: ^0.14.0
  window_manager: ^0.3.7
  
  # Utils
  shared_preferences: ^2.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
  # assets:
  #   - images/
